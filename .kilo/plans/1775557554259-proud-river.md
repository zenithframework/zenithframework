# Plan: Resolve Packagist Vendor Name Conflict

## Goal
Resolve the Packagist vendor name conflict by changing `zen/framework` to `zanith/framework`.

---

## Changes Required

### 1. Update composer.json
Change vendor name from `zen` to `zanith`:
```json
"name": "zanith/framework"
```

### 2. Update all PHP namespace imports (if any)
Search and replace `Zen\` with `Zanith\` in:
- All PHP files using namespace prefixes
- Check autoloader configuration

### 3. Update CLI tool references
- `zen` CLI file - update namespace imports if needed

### 4. Update documentation files
Update any references to `zen/framework` in:
- README.md
- COMPARE.md
- GUIDE.md

---

## Files to Modify
1. `composer.json` - Change name to `zanith/framework`
2. `boot/Autoloader.php` - Update namespace prefix if needed
3. `zen` CLI file - Update imports if needed
4. `README.md` - Update any zen/framework references
5. `COMPARE.md` - Update any references
6. `GUIDE.md` - Update any references

---

## Note
- Framework internal namespaces (e.g., `Zen\Routing\Router`) can remain unchanged - only the Packagist vendor name needs to change
- The CLI command `php zen` will still work since it references the internal namespace structure

---

## Completion Criteria
- [x] composer.json uses `zanith/framework`
- [x] README.md updated with zanith/framework
- [x] Lint passes
- [x] Ready for publishing to Packagist
