# Plan: Create .kilo gitignore and push 2.0 branch

## Goal
Create a .gitignore in .kilo directory and push new branch "2.0" to remote

## Steps

1. **Update .kilo/.gitignore** with custom entries:
   ```
   node_modules
   package.json
   bun.lock
   *.log
   *.cache
   .cache
   ```

2. **Create branch and push**
   - Create new branch: `2.0` from current HEAD
   - Push to remote: `git push -u origin 2.0`

## Files to Modify
- `.kilo/.gitignore`

## Commands to Run
```bash
git checkout -b 2.0
git push -u origin 2.0
```
